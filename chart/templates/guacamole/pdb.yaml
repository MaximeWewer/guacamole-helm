{{- if and .Values.guacamole.enabled .Values.guacamole.pdb.enabled -}}
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: {{ include "guacamole.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "guacamole.client.labels" . | nindent 4 }}
spec:
  {{- if .Values.guacamole.pdb.minAvailable }}
  minAvailable: {{ .Values.guacamole.pdb.minAvailable }}
  {{- else if .Values.guacamole.pdb.maxUnavailable }}
  maxUnavailable: {{ .Values.guacamole.pdb.maxUnavailable }}
  {{- else }}
  minAvailable: 1
  {{- end }}
  selector:
    matchLabels:
      {{- include "guacamole.client.selectorLabels" . | nindent 6 }}
{{- end }}
